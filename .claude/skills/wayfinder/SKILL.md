---
name: wayfinder
description: Plan a huge chunk of work — more than one agent session can hold — as a shared map of decision tickets on your issue tracker, and resolve them one at a time until the way to the destination is clear.
disable-model-invocation: true
---

Project adapter for Matt Pocock's `/wayfinder`. Follow that skill. This file only names where this repo keeps the map.

Tracker: `docs/agents/issue-tracker.md`. Active map: `.scratch/betterrx-weekend-demo/map.md`. Glossary: `CONTEXT.md`.

Plan, don't do. Each ticket is a question whose resolution is a decision. Do not write product code on this map. When the map is clear, hand off to `/to-spec`, not `/implement`.
