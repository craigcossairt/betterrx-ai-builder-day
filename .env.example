# Copy to .env.local. Never commit real keys.

NEXT_PUBLIC_SUPABASE_URL=https://nvkjnzagfwvltzpsxfcd.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Apply the schema once:
#   psql "$DATABASE_URL" -f supabase/migrations/0001_hospice.sql
# or paste that file into the Supabase SQL editor.
# The app keeps an in-memory store when these are empty.
